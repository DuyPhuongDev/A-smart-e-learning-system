<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.hcmut.lms</groupId>
    <artifactId>lms-backend</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>LMS Backend - HCMUT</name>
    <description>Learning Management System Backend - Microservices Architecture</description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.0</version>
        <relativePath/>
    </parent>

    <modules>
        <!-- Infrastructure Services -->
        <module>eureka-server</module>
        <module>api-gateway</module>
        <module>config-server</module>
        
        <!-- Shared/Common Module -->
        <module>common</module>
        
        <!-- Core Services -->
        <module>authentication-service</module>
        <module>user-management-service</module>
        
        <!-- Course Related Services -->
        <module>course-management-service</module>
        <module>course-delivery-service</module>
        <module>enrollment-service</module>
        
        <!-- Assessment Services -->
        <module>assessment-management-service</module>
        <module>assessment-execution-service</module>
        
        <!-- Student Services -->
        <module>student-personalization-service</module>
        <module>tracking-service</module>
        
        <!-- Communication Services -->
        <module>notification-service</module>
        <module>communication-service</module>
        
        <!-- Analytics Service -->
        <module>analytics-reporting-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2025.0.0</spring-cloud.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud Dependencies -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            
            <!-- Common Module -->
            <dependency>
                <groupId>com.hcmut.lms</groupId>
                <artifactId>common</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
        </repository>
        <repository>
            <id>spring-snapshots</id>
            <name>Spring Snapshots</name>
            <url>https://repo.spring.io/snapshot</url>
        </repository>
    </repositories>

</project>
